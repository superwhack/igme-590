using UnityEngine;
using System.Collections.Generic;

public class InventoryItemShape : MonoBehaviour
{
    InventoryItem item;

    [SerializeField]
    List<InventoryItemTile> tiles;

    Vector2Int center;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    void BuildTiles()
    {
        
    }
}
